{
  "name": "content-streaming",
  "version": "1.0.0",
  "description": "",
  "main": "dist/app.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "nodemon --watch 'src/**/*' --ext '*' --exec 'tsc -p tsconfig.json && yarn start'",
    "start": "node dist/app.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "range-parser": "^1.2.1"
  },
  "devDependencies": {
    "@types/range-parser": "^1.2.4",
    "@types/express": "^4.17.11",
    "typescript": "^4.3.5"
  }
}
